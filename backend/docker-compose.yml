version: '3.8'
services:
  api:
    build: .
    ports:
      - "3001:3001"
    volumes:
      # 🟢 บรรทัดนี้คือหัวใจสำคัญ!
      # ซ้าย (./data) = โฟลเดอร์ในเครื่องคุณ
      # ขวา (/app/data) = โฟลเดอร์ใน Docker ที่เราแก้โค้ดไปชี้เมื่อกี้
      - ./data:/app/data 
    environment:
      - PORT=3001