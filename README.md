# Audio Annotation Tool 🎧

เครื่องมือสำหรับตรวจสอบ (Verify), แก้ไข (Edit), และจัดการข้อมูลคู่เสียงและข้อความ (Audio-Text Pairs) สำหรับงาน Speech Recognition หรือ TTS มาพร้อมกับหน้าตา UI แบบ **Minimalist Pastel** ที่ดูสะอาดและสบายตา

## ✨ ฟีเจอร์หลัก (Features)

* **Clean UI:** ดีไซน์ใหม่ สไตล์มินิมอล สีพาสเทล แยกหมวดหมู่ด้วยสี (Pending: ม่วง, Correct: เขียว, Fail: แดง)
* **Audio Player:** ตัวเล่นเสียงแบบ Slider พร้อมปุ่ม Play/Pause และแสดงเวลา
* **Tokenization Support:** ระบบตัดคำ (Word Tokenization) เพื่อช่วยตรวจสอบความถูกต้องของประโยค (แสดงผลแบบการ์ดเรียงสวยงาม)
* **Keyboard Shortcuts:** รองรับการทำงานด้วยคีย์บอร์ดเพื่อความรวดเร็ว (Enter = Correct, Backspace = Incorrect/Fail)
* **Data Management:**
    * โหลดไฟล์ Metadata (TSV) และสแกนไฟล์เสียงจาก Folder อัตโนมัติ
    * แยกสถานะไฟล์เป็น Pending, Correct, และ Fail
    * บันทึกไฟล์ `Correct.tsv` และ `fail.tsv` อัตโนมัติ
    * บันทึกประวัติการแก้ไขคำผิดลง `ListOfChange.tsv`

---

## 🛠️ Tech Stack

* **Frontend:** React, TypeScript, Tailwind CSS
* **Backend:** Node.js, Express, TypeScript
* **NLP Engine:** Python (PyThaiNLP) สำหรับการตัดคำ

---

## ⚙️ การติดตั้ง (Installation)

โปรเจกต์นี้แบ่งเป็น 2 ส่วนคือ `frontend` และ `backend` ต้องรันทั้งคู่พร้อมกัน

### 1. เตรียม Environment
* Node.js (v16 ขึ้นไป)
* Python (v3.8 ขึ้นไป)
* ติดตั้ง Python Library ที่จำเป็น (สำหรับตัดคำ):
    ```bash
    pip install pythainlp flask cors
    ```
    *(หมายเหตุ: ตรวจสอบ requirements.txt ใน backend อีกครั้งหากมี)*

### 2. ติดตั้งและรัน Backend
```bash
cd backend
npm install
npm start